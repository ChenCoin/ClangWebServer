main.c 是一个Clang的web服务器的基本框架，仅用不到100行代码完成。

当然，仅有最基本的功能，达到可用的水平，与真正的web服务器的距离还非常大。

考虑到这个项目的目的是一个文字冒险游戏，静态网页是可以在编译时就确定的，所以动态没有读取网页的功能。

作为一个文字游戏的Web服务器框架也勉强足够了。

静态编译：gcc main.c -static -o app

在8080端口运行：./app 8080
